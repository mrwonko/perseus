impure function fib( x : i32, print_all : bool ) -> i32
{
    let result =
        if x <= 1
            x
        else
            ( fib( x - 1, print_all ) ) + ( fib( x - 2, false ) );
    if print_all
        print( result );
    result
}

impure function main() -> i32
    fib( 6, true )
