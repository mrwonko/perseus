function fib( x : i32 ) -> i32
    if x <= 1
        x
    else
        // these parens are required because function calls are treated as an operator, and operator precedence is not yet resolved. it's a bit silly.
        ( fib( x - 1 ) ) + ( fib( x - 2 ) )

function main() -> i32
    fib( 6 )
